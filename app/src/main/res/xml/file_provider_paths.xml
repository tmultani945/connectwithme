<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Reflection share cards written to context.cacheDir/share_cards/*.png and
         exposed to other apps via content://${applicationId}.fileprovider/. -->
    <cache-path name="share_cards" path="share_cards/" />
</paths>
